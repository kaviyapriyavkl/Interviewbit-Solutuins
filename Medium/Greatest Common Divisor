public class Solution {
    public int gcd(int A, int B) {
     int gcd = 0;
     if(B==0){gcd=A;}
     else{
         for(int i=1; i<=A && i<=B;i++)
         {
             if(A%i==0 & B%i==0)
             {
                 gcd=i;
             }
         }
     }
     return gcd;
    }
}
